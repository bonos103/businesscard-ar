version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    ports:
      - 8080:8080
    volumes:
      - ./app:/app
      - /app/node_modules
    command: yarn serve 

